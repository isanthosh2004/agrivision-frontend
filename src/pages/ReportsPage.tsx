export function ReportsPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold">Reports</h1>
      <p className="mt-2 text-sm text-white/70">Generate PDFs via `/api/reports/generate`.</p>
    </div>
  );
}
