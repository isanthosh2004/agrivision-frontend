export function RiskScorerPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold">Risk Scorer</h1>
      <p className="mt-2 text-sm text-white/70">Use the Crop Planner page parameters, then wire `/api/risk/analyze` similarly.</p>
    </div>
  );
}
